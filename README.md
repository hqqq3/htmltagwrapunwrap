# HTML Tag Wrap Unwrap

VS Code 插件，用于 HTML 标签的包裹和删除操作。

## 测试方法

1. 按 `F5` 启动扩展开发宿主
2. 打开一个新的 VS Code 窗口
3. 在编辑器中按 `Alt + D`，应该会弹出提示"删除标签命令已触发"
4. 在编辑器中按 `Alt + W`，应该会弹出提示"包裹标签命令已触发"

## 快捷键

- `Alt + D` - 删除标签
- `Alt + W` - 包裹标签
